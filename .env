# service-compose 环境配置
# 后端 API 监听地址
HOST=0.0.0.0
# 后端 API 监听端口
PORT=8080
# 服务配置文件路径（支持 .yaml 和 .json）
CONFIG_FILE=examples/services.yaml
# auth.db 存储路径（绝对路径或相对于项目根目录，默认为 logs/auth.db）
# AUTH_DB_PATH=
